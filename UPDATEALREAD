-- Gui to Lua
-- Version: 3.2
--New instance

-- Instances:

local ScreenGui2 = Instance.new("ScreenGui")
local Frame = Instance.new("Frame")

--Properties:

ScreenGui2.Name = "ScreenGui2"
ScreenGui2.Parent = game.Players.LocalPlayer:WaitForChild("PlayerGui")
ScreenGui2.ZIndexBehavior = Enum.ZIndexBehavior.Sibling

Frame.Parent = ScreenGui2
Frame.BackgroundColor3 = Color3.fromRGB(255, 255, 255)
Frame.Size = UDim2.new(0, 100, 0, 100)
Frame.Visible = false

-- Scripts:

local function QONY_fake_script() -- Frame.LocalScript 
	local script = Instance.new('LocalScript', Frame)

	local m = game.Players.LocalPlayer:GetMouse()
	
	
	m.Button1Down:Connect(function()
	
		game.Players.LocalPlayer.Character.HumanoidRootPart.CFrame = CFrame.new(0,-200,0)	
		wait(0.3)
		game.Players.LocalPlayer.Character.Humanoid.Health = 0
		script.Disabled = true
		script:Destroy()
	end)
end
coroutine.wrap(QONY_fake_script)()
local function QCYOP_fake_script() -- ScreenGui2.LocalScript 
	local script = Instance.new('LocalScript', ScreenGui2)

	
end
coroutine.wrap(QCYOP_fake_script)()
